title: "Yogi & Linga"
description: "Sexual wellness, high-tech toys & science-backed self-care for curious adults."
url: "https://sontis98.github.io"
baseurl: "/yogilinga"
author: "Yogi & Linga Editorial"
email: "hello@yogilinga.com"
lang: "en-US"

# Build
markdown: kramdown
highlighter: rouge
permalink: /:categories/:title/

# Include _pages directory so Jekyll processes it
include:
  - _pages

# Pagination
plugins:
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-seo-tag
paginate: 9
paginate_path: "/page/:num/"

# Collections
collections:
  reviews:
    output: true
    permalink: /reviews/:title/
  guides:
    output: true
    permalink: /guides/:title/

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      author: "Editorial Team"
  - scope:
      path: ""
      type: "reviews"
    values:
      layout: "review"
  - scope:
      path: ""
      type: "guides"
    values:
      layout: "guide"

# Exclude
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - brain/
